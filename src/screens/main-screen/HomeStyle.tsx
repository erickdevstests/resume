import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderWidth: 5,
    justifyContent: 'flex-start',
    alignItems: 'center'
  },
  img: {
    width: 120,
    height: 120,
    marginBottom: 30
  },
});